// conditional statement

// let age = prompt("Enter your age"); used to take user input
// prompt always a string as input so in case of integer we need to typecast them to integers
// age=Number.parseInt(a);
let age = 23;
if (age > 18 && age < 75) {
  console.log("Drive Safe");
} else {
  console.log("Dont Drive");
}
